# Changelog

本文件记录 DeepSeek Harness Studio 的重要变更。各版本的详细发布说明见 `docs/RELEASE_NOTES_*.md`。

## [Unreleased]

## [1.07.6] - 2026-08-24

### 🧩 插件安装兼容性
- DSH 只返回 `pnpm failed in profile directory` 通用错误时，也会自动切换 `https://registry.npmmirror.com` 重试，不再依赖底层错误必须包含 `ECONNRESET` 等代码。
- pnpm 子进程输出改用连续 UTF-8 解码，避免中文恰好跨输出分块时显示为 `����`。
- 镜像重试仍失败时，提示会明确说明 npm 官方源和国内镜像均已尝试，并清理无效替换字符。
- 新增通用 DSH pnpm 错误回退和错误文本清理测试。

## [1.07.5] - 2026-08-24

### 🌐 跨设备插件安装
- 验证安装包不包含开发机盘符、用户名或工程目录硬编码；Node、DSH CLI、pnpm shim 与 profile 均根据当前设备动态解析。
- pnpm shim 会在安装目录变化后自动重写，迁移 `.dsh` 或覆盖安装不会继续引用旧电脑路径。
- 插件命令遇到 npm 官方源连接重置、超时、DNS 或网络不可达错误时，自动切换 `https://registry.npmmirror.com` 重试。
- 插件活动日志新增实际 Node 与 profile 路径，便于跨设备排查。
- 新增安装目录迁移和国内 npm 镜像回退测试。

## [1.07.4] - 2026-08-23

### 🧩 插件中心
- 重构插件清单：读取已安装包的真实版本、说明、来源、安装目录和 `dsh.bundle.patch`，区分已启用、已停用、文件缺失、不兼容、清单损坏与孤立 bundle。
- 新增插件筛选、单插件更新/修复、profile 环境修复、打开安装目录、自定义来源预检和独立活动日志。
- 精选生态组件会显示实际状态；已停用组件可一键重新启用，异常组件会给出明确修复入口。
- 插件操作通过结构化状态展示当前动作与目标；即使运行时停止失败，也会正确解除忙碌状态并尝试恢复 Harness。
- 新增“启动诊断”：解析 bundle patch 并验证其中的 loader 模块是否能从当前 profile 加载；故障插件会被安全隔离但不会卸载。
- 安装、更新和修复后自动执行同一检查，避免单个社区插件导致整个 Harness 插件树无法启动。
- Harness 重启现在必须通过运行状态验证，启动失败会显示真实运行时错误，不再把“命令已执行”误报为成功。
- 暂时移除 `@paicat1/dsh-screenshot@1.0.0` 精选入口：该版本的 patch 引用了不存在的 `dsh-screenshot` loader，会阻断 DSH 启动；已安装副本将保留并标记为“已隔离”。
- 应用启动和手动重启都会先运行插件自检；若静态清单正常但 Harness 仍无法启动，Studio 会通过真实启动探测分组定位故障组件，并自动恢复其余可用组件。
- 隔离结果持久保存到 web profile 的 `.studio-quarantine.json`。后续安装其他组件时，即使 DSH 包管理器重新生成 bundle 列表，已知故障插件也不会被意外重新启用。

### 🔒 安全与验证
- 本地插件在安装前必须包含有效 `package.json`、包名、`dsh.bundle.patch`、实际 patch 文件，以及可以解析的 loader 模块。
- 拒绝伪装成包来源的命令行选项，插件包名与打开目录操作均使用严格校验。
- 新增插件健康、来源预检、更新/修复命令与输入校验回归测试。
- 新增实际启动隔离和“包管理器重新写回故障 bundle”回归测试。

## [1.07.3] - 2026-08-21

### 🐛 修复模型切换
- 修复使用 ModLens 图片模型后，因会话历史包含图片而无法切回普通 DeepSeek 文本模型的问题。
- 切换到文本模型时保留原始文字与 ModLens 识别结果，仅把历史图片数据转换为明确的文字占位，避免把不支持的图片内容发送给纯文本 API。
- 输入框中尚未发送的新图片继续由图片能力检查保护，避免误用纯文本模型。

### 🧰 构建与验证
- 新增可重复执行的 DSH 兼容补丁，在依赖安装和发布构建前自动应用。
- 新增模型切换回归测试，并同步更新 README、安装指南与 GitHub Release 发布文案。

## [1.07.2] - 2026-08-xx

### 🔧 更新方式调整
- 移除静默自动更新，改为「下载安装包 + 安装向导」手动覆盖安装（可自定义安装目录），更稳定可靠。

## [1.07.1] - 2026-08-xx

### 🐛 修复静默更新
- 修复「立即更新」后应用直接退出、更新未生效的问题：不再通过 PowerShell 中间进程启动安装程序，改为直接以 `/S --updated` 启动 NSIS 安装程序。
- 安装包恢复为 one-click 一键静默安装（默认安装到用户目录），静默覆盖升级更可靠。

## [1.07.0] - 2026-08-xx

### 📄 文档读取（PDF / Word / Excel）
- 生态组件新增「文档读取」组件（`dsh-plugin-doc-reader`），为模型增加 `read_document` 工具。
- 支持按文件路径读取 PDF、Word（docx）、Excel（xlsx）与纯文本，任意模型模式均可调用。
- 补充使用说明：聊天输入框只收图片（PNG/JPG/WebP/GIF），读文档请用「文件路径 + read_document」。

### 🧭 使用说明
- 明确「含图片的会话切换到纯文本模型会被拒绝」是 Harness 的安全机制，并给出规避方式（有图用 `(modlens vision)` 模型 / 新建会话）。

### 📦 其他
- 版本升级至 1.07.0。

## [1.06.0] - 2026-08-xx

### 🖼️ 视觉模块简化（默认阿里千问）
- 「视觉引擎」默认只展示「阿里千问 Qwen-VL」，其余引擎（Gemini、Anthropic、Claude CLI、OpenAI 兼容等）折叠进「高级选项」，避免 Claude/codex 等选项造成困惑。
- 未配置时默认引导到阿里千问：只需粘贴阿里云百炼 DashScope API Key 即可识图。

### ⚡ 静默覆盖升级
- 更新改为静默覆盖：点击「立即更新」后自动覆盖安装到原目录并重启应用（`/S --updated`），不再弹出安装向导；首次安装仍是向导式，可选择安装目录。
- 更新前应用自动退出，安装完成后自动重启；工作区、会话、插件与设置均保留。

### 📦 其他
- 版本升级至 1.06.0（版本号规整：1.6 → 1.06，中位补零，与 1.10 等后续版本对齐）。

## [1.05.0] - 2026-08-21

### 🖼️ 视觉模块重做（接入阿里千问）
- 新增「阿里千问 Qwen-VL」视觉引擎快捷配置：接入阿里云百炼 DashScope（OpenAI 兼容端点），国内直连、无需代理。
- 自动填充 `https://dashscope.aliyuncs.com/compatible-mode/v1` 与 `qwen-vl-max`，只需粘贴 API Key 即可用。
- 保留 OpenAI 兼容、Gemini、Anthropic、Claude CLI、Antigravity CLI 引擎与自动故障转移。

### 💰 Token 余额显示
- 顶栏实时显示 DeepSeek 账户余额（读取 `~/.dsh/.credentials.yaml` 中的 `DEEPSEEK_API_KEY`），点击可刷新。
- 未配置 API Key 时明确提示；密钥不进入界面回显。

### ⚡ 更新器网络优化
- 更新下载自动走系统代理（环境变量优先，其次 Windows 系统代理）。
- 元数据请求超时 15s→30s、安装包下载超时 30s→180s。
- 保留国内社区镜像优先、GitHub 自动回退与 SHA-256 强制校验。

### 📦 构建与发布
- 新增 `scripts/publish-release.cjs` 一键发布脚本（凭据来自 Git Credential Manager）。
- 版本升级至 1.05.0。

## [1.04.1] - 2026-08-xx

### 国内更新线路
- 新增"自动（国内优先）"下载模式，内置社区镜像失败后自动回退 GitHub 官方线路。
- 新增"仅 GitHub"和"自定义镜像"模式。
- 下载界面实时显示当前线路；线路失败后自动尝试下一条线路。
- 任一线路发生 SHA-256 不一致时立即丢弃文件，只有匹配官方校验值的安装包才能启动。

## [1.04.0] - 2026-08-xx

### 视觉 API 配置
- ModLens 视觉 API 设置：配置 Gemini、Anthropic 或任意 OpenAI 兼容多模态端点。
- 检测引擎状态并在保存后自动重启 Harness；API Key 不进入项目或 Git。
- 安全策略与故障说明见 `docs/RELEASE_NOTES_1.04.0_CN.md`。

### 其他
- 社区插件中心、Skill 管理、插件启用/停用/卸载、路径识别、安装版与便携版等基础能力（详见 README）。
